apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "instagram-monitor.name" . }}-config
  labels:
    {{- include "instagram-monitor.labels" . | nindent 4 }}
data:
  config.json: |
    {{- include "instagram-monitor.config_json" . | nindent 4 }}
